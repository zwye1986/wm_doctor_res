<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.pinde.res.dao.xnres.ext.XnresDiscipleInfoExtMapper" >
	<select id="getTeacherNames" parameterType="string" resultType="java.util.HashMap">
		SELECT  LISTAGG(TEACHER_NAME,'，') WITHIN GROUP( ORDER BY CREATE_TIME) TEACHER_NAMES
    FROM RES_STUDENT_DISCIPLE_TEACHER T
    WHERE RECORD_STATUS='Y' AND DOCTOR_FLOW=#{userFlow}
    GROUP BY DOCTOR_FLOW
	</select>
</mapper>